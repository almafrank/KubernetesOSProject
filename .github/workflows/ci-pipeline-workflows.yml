name: Build unittest and Build and Push Containers to GHCR
#trigger feature branch
on:
  push:
    branches:
      - feature/*

  pull_request:
    branches:
      - main

#bygga postgres image
jobs:

  build-unittest-backend:
    runs-on: ubuntu-latest

    steps:
        # Checkout repo
        - name: Checkout repo
          uses: actions/checkout@v4

        # Set up Go
        - name: Set up Go
          uses: actions/setup-go@v4
          with:
            go-version: 1.24.12

        # Go command Download Go 
        - name: Download Go Dependencies
          working-directory: backend
          run: go mod download

        # Run unittest in backend/main-test.go:
        - name: Run unittests
          working-directory: backend
          run: go test -v ./...

  
  build-postgres-container-image:
    runs-on: ubuntu-latest
    needs: build-unittest-backend
    permissions:
      contents: read
      packages: write


    
    steps:
   #Kollar repot
      - name: Checkout repo
        uses: actions/checkout@v4

    # Logga in på Github för att pusha och pulla images i Container Registry
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN}}
    
    #Bygger postgres-db image med container
      - name: Build and push postgres image to Container Registry
        run: |
          docker build -f postgres/Containerfile -t ghcr.io/almafrank/postgres-guestbook:latest postgres
          docker push ghcr.io/almafrank/postgres-guestbook:latest

  build-redis-container-image:
    needs: build-postgres-container-image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write


    
    steps:
    #Kollar repot
      - name: Checkout repo
        uses: actions/checkout@v4

    # Logga in på Github för att pusha och pulla images i Container Registry
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.TOKEN2026 }}
    
    #Bygger redis image med container
      - name: Build and push redis image to Container Registry
        run: |
          docker build -f redis/Containerfile -t ghcr.io/almafrank/redis-guestbook:latest redis
          docker push ghcr.io/almafrank/redis-guestbook:latest


  build-backend-container-image:
    needs: build-redis-container-image
    
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write


    
    steps:
   #Kollar repot
      - name: Checkout repo
        uses: actions/checkout@v4

    # Logga in på Github för att pusha och pulla images i Container Registry
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.TOKEN2026}}
    
    #Bygger backend image med container
      - name: Build and push backend image to Container Registry
        run: |
          docker build -f backend/Containerfile -t ghcr.io/almafrank/backend-guestbook:latest backend
          docker push ghcr.io/almafrank/backend-guestbook:latest
    
  build-frontend-container-image:
    needs: build-backend-container-image
    
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write


    

    steps:
   #Kollar repot
      - name: Checkout repo
        uses: actions/checkout@v4

    # Logga in på Github för att pusha och pulla images i Container Registry
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.TOKEN2026 }}
    
    #Bygger frontend image med container
      - name: Build and push frontend image to Container Registry
        run: |
          docker build -f frontend/Containerfile -t ghcr.io/almafrank/frontend-guestbook:latest frontend
          docker push ghcr.io/almafrank/frontend-guestbook:latest
      
  
  